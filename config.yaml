device: cpu

model:
  num_encoding_layers: 2
  dropout: 0.1

train:
  epochs: 10
  train_batch_size: 32
  validation_batch_size: 32
  base_lr: 0.0003
  weight_decay: 0.00005
  checkpoint_dir: 'checkpoints'